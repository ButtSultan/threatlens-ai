name: ThreatLens AI — CD Pipeline

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: CD Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "CD pipeline runs manually only"
