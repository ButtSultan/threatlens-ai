# ThreatLens AI — Frontend Development Environment
# Used when running: npm start
# These values point to the local Docker backend

REACT_APP_API_URL=http://localhost/api/v1

# Disable browser opening automatically
BROWSER=none
